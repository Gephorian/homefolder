
[[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile
[[ -s "$HOME/.bashrc" ]] && source "$HOME/.bashrc" # Load the default .profile
[[ -s "$HOME/.cargo/env" ]] && source "$HOME/.cargo/env" # Load rust env
